services:
  - type: web
    name: angels-school
    env: node
    plan: free
    nodeVersion: 20.15.0   # or the version you run locally (check with node -v)
    buildCommand: pnpm install && pnpm build
    startCommand: pnpm start
    envVars:
      - key: NODE_ENV
        value: production
